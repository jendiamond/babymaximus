<%= form_for(sleep) do |f| %>
  <% if sleep.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sleep.errors.count, "error") %> prohibited this sleep from being saved:</h2>

      <ul>
      <% sleep.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :day_id %>
    <%= f.text_field :day_id %>
  </div>

  <div class="field">
    <%= f.label :sleep_start %>
    <%= f.datetime_select :sleep_start %>
  </div>

  <div class="field">
    <%= f.label :sleep_end %>
    <%= f.datetime_select :sleep_end %>
  </div>

  <div class="field">
    <%= f.label :notes %>
    <%= f.text_area :notes %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
